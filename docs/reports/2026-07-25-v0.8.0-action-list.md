# v0.8.0 — What Is Left, Ranked

> Last updated: 2026-07-28 · commit `3eefaf1ed`

Consolidated from the PR review pass, the MTP root-cause measurement, and
the vLLM comparison. Everything here is measured or read out of source;
inferences say so.

Companions: `2026-07-25-paged-gate-results.md` (gates, vLLM diff, MTP
measurement), `../design/paged-kv-migration.md` (the plan, with
correction log).

---

## Release posture

**Two PRs, one accumulator.** `release/v0.8.0` on both repos, branched from
`master`/`main`. `d-inference#590` and `mlx-swift-lm#95` target it. Master
is untouched.

The other 19 paged PRs are closed as superseded, each with its
carry-forward items named in the close comment. `#306` is closed (its
entire target surface was deleted in v0.7.5). `#558` is deferred, not
closed.

---

## BLOCKING — must land before the release ships

### 1. Split `BenchCBv2` so swift-testing runs again

**728 `@Test` entries across 67 files are unreachable.** `Tests/BenchCBv2Tests`
depends on the BenchCBv2 *executable* target, so SwiftPM routes the
swift-testing pass at that binary, which rejects `--test-bundle-path` and
exits. Only XCTestCase classes execute.

`swift test` **exits 0** in that state, so four CI gates — eligibility,
backend, kernel, safety — have been reporting green while running nothing.
Those are precisely the gates added because "the paged-KV migration had no
automated numerical gate".

Verified with a control: `CBv2PagedEligibilityTests` → `Executed 0 tests`,
exit 0; `WiredMemoryPolicyTests` (XCTestCase) → 4 tests.

**The code is sound.** Removing the offending target locally makes all four
reachable: **70 tests in 5 suites pass.** The evidence is recoverable; only
the harness was broken.

A tripwire is already landed (`0ac056b0c`) failing those steps on a zero
executed count. The structural fix — library target plus executable shim —
is in flight.

### 2. The flip was made, and REVERTED on measurement

`.auto` resolves **contiguous**. A flip to paged landed and was reverted
the same day, and the reason is a hard blocker rather than a tuning
question.

**Paged adoption is not exact against paged cold.** Measured on
**`mlx-community/gemma-4-e2b-it-4bit`** with **28,416 CACHED tokens**
(cite the cached figure, not a prompt length — that is what was on the
wire), one process, one paged slot, backend label confirmed on every
sample. Divergence at **completion token 20 of 32**, after 20
byte-identical tokens.

**The adopted span is ~50%, not the ~10% that circulated.** The 25,600
replay bound is the **26B** figure and was mistakenly applied to e2b —
by three people including me. `cbv2RequiredRecompute` is
`windowCount × maxWindow` over the sliding layers, verified against the
real configs:

    e2b : 28 sliding x  512 = 14,336  ->  at 28,416 matched, 14,080 adopted = 49.6%
    26B : 25 sliding x 1024 = 25,600  ->  at 28,672 matched,  2,816 adopted =  9.9%

So the e2b divergence is sourced from roughly HALF the matched span, which
makes it materially stronger evidence than the "thin sample" framing
suggested. It also means **26B is the thin one** — any 26B measurement at
28,672 has ~90% replayed cold arithmetic that cannot diverge by
construction, so a 26B *exact* result there is close to uninformative and
must not on its own reopen this decision. Reaching e2b's regime on 26B
needs matched ≈ 51k.

**And one-sidedness is the finding, not the flip.** On gpt-oss with a
valid 48-token window the asymmetry INVERTS — contiguous
`adoptionExact=FALSE`, paged `TRUE` — and gpt-oss's precision control is
token-exact over 144 tokens, so that is a real result rather than
numerics. "Adopted output can differ from cold output" is therefore not a
paged-only property. The revert stands on contiguous being exact on e2b
in all three arms while paged was not, on the same prompt; it does not
stand on the bare existence of a flip.

Both remaining checkpoints are precision-sensitive by the fp16→fp32
control (26B: 2/3 rows at token 1; e2b: 1/3 rows at token 36 once chat
templating gave it a 121-token sample instead of 3). Only gpt-oss excludes
precision.

> **Model attribution, corrected.** The revert commit `313dfc2ea` says
> "gemma-4-26B". That is WRONG. `e2e/exact_cache_routing_test.go:304`
> overrides the testbed default and returns `gemma-4-e2b-it-4bit`; the 26B
> figure came from the config default at `e2e/testbed/config.go:36`, which
> this test does not use. The decision is unaffected — the asymmetry is
> 3/3 contiguous PASS against 3/3 paged FAIL on the *same* model — but
> anyone reproducing it needs the right checkpoint, and a separate
> `--parity` run on 26B is NOT the same experiment.

    paged      cold    -> "...many times"     deterministic, run TWICE, byte-identical
    paged      adopted -> "...What would..."  differs from its OWN cold, token 20 of 32
    contiguous cold    -> "...What would..."
    contiguous adopted -> "...What would..."  exact

Nine observations, nine for nine: every PASS contiguous, every FAIL paged.
Cold prefill is deterministic, so this is adoption — not accumulation
noise, not load sensitivity.

Note WHICH answer paged-adopted gives: **the contiguous one**, not its own
paged-cold one. Frozen replay reads cached keys; paged cold projects fresh
through `PagedLayerCache.prefillKVWritingChunk`. The two paths through
paged disagree.

**The framing matters more than the fact.** Paged cold is deterministic,
B=8 throughput is better (1.27× vs contiguous's 1.07×), prefill is faster.
Anyone who records this as "paged wasn't ready" will re-flip it the moment
benchmarks look good, reasoning correctly from a false premise. The
disqualifier is narrow: output depends on prefix-cache state the caller
cannot see.

Suspect, narrowed and engine-side: the deleted extra prefill chunk in the
paged frozen bound (`PrefixReusePlan.swift:120-152`,
`PagedKVBackend.swift:301-319`), whose own comments warn the bound is then
"short by one chunk and it will accept a replay that leaves the sliding
rows silently inexact". The WS-4.2 sidecar path is eliminated — its env
var is opt-in and nothing in the repo sets it.

Two things that made this decisive rather than arguable:
- **CI never exercised paged on this suite.** At PR head `.auto` was still
  contiguous, so all 77 green runs are contiguous results. Absence of
  evidence, not evidence of absence.
- **gemma-4's argmax is near-tie unstable** — a benign fp16→fp32 pool
  change flips 2 of 3 parity rows at token 1, and on one row paged-fp32
  reproduces the contiguous token exactly. That explains why a path
  difference becomes visible; it does not excuse shipping one by default.
  On gpt-oss the same control is token-exact over 144 tokens.

**The opt-in carries the defect too.** A fourth control arm settled the
question the first three left open — `engine_v2_kv_backend = "paged"`
explicitly, not `.auto`-resolved, and it FAILS identically. Full matrix,
every arm backend-labelled:

| arm | resolved | assertion |
|---|---|---|
| `.auto`, no env | **paged** | FAIL |
| `DARKBLOOM_CBV2_PAGED_KV=0` | contiguous | PASS |
| explicit `contiguous` | contiguous | PASS |
| explicit `paged` | **paged** | FAIL |

3/3 contiguous PASS, 3/3 paged FAIL, same two strings and the same byte
offset every time. So this is a defect in the FEATURE, not merely a wrong
default: an operator who opts into paged gets the same non-transparency.
Say so wherever the opt-in is offered — it is a real escape hatch for
throughput, and it is not exact under prefix reuse.

**The adoption oracle cannot see this defect yet.** `probePrefixReuse`
submits both probe requests with `maxTokens: min(8, ...)`
(`BackendParityHarness.swift:733`, `:748`), so it compares EIGHT tokens.
The divergence is at completion token 20 of 32, after 20 byte-identical
tokens — the comparison window closes twelve tokens before the flip can
appear. An `adoptionExact=true` from that probe is not weak evidence, it
is NO evidence. Being fixed: the cap becomes operator-controlled and the
verdict discloses how many completion tokens it actually covered.

That is the sixth instance of the release's pattern, and a new axis of it:
not a proxy this time but an under-sampled window. The corollary worth
keeping alongside the proxy question — **what SAMPLE is the observation
drawn from, and does the verdict disclose it?**

**Re-flip criterion:** paged adopted == paged cold on that prompt. Still
also needs the G3/G4 24-hour soaks, which cannot run on one machine.

---

## NOT blocking — but do not lose

### 3. Paged MTP: batch the verify columns

Measured: paged verify costs **+2.217 ms** vs contiguous at k=3, and the KV
layer accounts for **100.4%** of the reported engine gap. Mechanism is
dispatch count — `2 × (1+k) × layers` against `(1+k) × layers`.

The paged kernel has the *better* fixed cost (1.46 vs 2.83 ms) and a 2.55×
steeper slope; break-even is 1.52 columns. So paged wins at k=0 and loses
at every k ≥ 1.

Fix: hoist the KV write out of the column loop, pack columns onto the
kernel's batch axis. **No Metal changes.** ~150 lines, medium.

> **Land this BEFORE the depth-controller fix.** Fixing the controller
> alone pushes it toward k ≥ 1, which is exactly where paged is slower —
> regressing the backend the release is named for.

### 4. MTP depth controller: credit the seed's token

`CBv2MTPRoundDriver.swift:472` computes
`attributed = wallTimeNanos &+ claimedSeedCostNanos`, charging the seed's
full wall time to the verify round while `expectedCommitted` never credits
the token the seed emitted.

Consequence, derived and swept: depth 1 is selected iff `a·T > V₁`, and
since `V₁ > T` and `a ≤ 1` that is **unsatisfiable** — 9/9 parameter
combinations lose as written, 8/9 win once the token is credited. Worse,
`CostState.observe` sets `ewmaNanos = sample` on the first sample, and the
first sample at every depth is the seed-inflated one.

Worth **+14.7%** decode throughput on contiguous. ~5 lines.

### 5. Per-row ragged commit

`EngineLoopV2+MTPFinalize.swift` computes `commonEmitted` as the **min**
accept-prefix across the batch and truncates every row to it, discarding
tokens the target already confirmed. Modelled loss: 0% at B=1, 31% at B=2,
53% at B=4, **62% at B=8** — because `P(commonEmitted > 1) = pᴮ`.

The stated justification ("keeps quantized MoE target batches
shape-identical") appears exactly once in the tree, nothing depends on it,
and heterogeneous per-row KV lengths are already routine on both backends.

vLLM commits per request. Moderate, contained in one file.

### 6. Prefix-outcome telemetry defect

`EngineLoopV2.watchdogTick` and `forceFinishStreamsOnShutdownTimeout`
publish a bare `CBv2Usage`, whose default `prefixCacheOutcome` is
`.disabled`. `EngineV2Bridge+PrefixCache` maps `.disabled` into the same
bucket as `.skippedPolicy`, so **every watchdog- or shutdown-killed request
books its prefix outcome as "policy declined" on the wire** — inflating
exactly the metric anyone tuning prefix-cache policy would read.

There is already a compensating hack two cases above it (`.disabled` +
`hitTokens > 0` reinterpreted as `.hit`), which says someone met this
before and patched the symptom.

Multi-site because the outcome mutates after enqueue when adoption flips it
to `.hit`.

---

## From vLLM — ranked, and mostly cheap

Read against `vllm-project/vllm @ b153ae6089`. Full detail in the gate
results doc.

| # | change | shape |
|---|---|---|
| 1 | **Asymmetric watermark** — charge the reserve only to waiting/preempted rows | two branches |
| 2 | **Capture blocks as they fill** — publish the NAME inline in a not-ready state, materialise off-thread | three stages, no new field |
| 3 | **Bridge link 5** — `stagedWindow` is built, tested, and never called | 4 of 5 links already exist |
| 4 | Lift `L==1` on the paged kernel — kills the prefill gather copy | re-derive the threadgroup budget |

**The root cause underneath all of them:** we move KV *bytes* where vLLM
moves *references*. `cache_full_blocks` performs zero tensor operations.
Our 25,600-token reuse floor, the tiling requirement, the 200 MiB sidecars,
the 20 GiB ceiling, and `retainPage`'s zero callers are six symptoms of one
choice.

**Target gpt-oss first** — symmetric head geometry gives a flat 16-token
floor (1600× below ours), an 8-block contiguous run instead of 64, and 2
cache groups instead of 6.

**Do not build:** CoW, beam fork (`vllm/core/` is deleted), cascade
attention (needs ≥ 8 requests, gated off for sliding-window models), block
de-dup, SLO-aware admission. And vLLM does **not** solve long-prefill
delaying decodes either — its knob ships disabled, like ours.

---

## Carry-forward from the superseded PRs

Non-blocking, each verified live against the integration branch:

- `#582` testbed config logic has **zero tests** — and the PR body claimed a
  test that was never committed. Temp `StateDir` leaks on two error paths.
- `#583` the `gemma_contbatch` harness forwards neither new control, so it
  can never run a paged sweep — which quietly voids the `require_positive`
  mitigation `#586`'s partial-refusal design leans on.
- `#590` `commitSlabs()` does not recheck headroom before
  `materializeSlabs()`.
- `#590` speculative backup never re-latches `servedKVBackend`.
- `#590` `lastModelLoadError` never expires → permanent doctor FAIL.
- `#585` `darkbloom start --local` never surfaces the retired-key warning.

---

## Process notes worth keeping

**Every gate that failed us measured a PROXY for the thing it was named
for.** Four instances in one release:

| criterion | proxy it measured | thing it was named for |
|---|---|---|
| prefix reuse | tokens SAVED | whether skipping changed the answer |
| MTP exactness | drafts inherited | MTP correctness |
| four CI steps | `exit 0` | tests actually running |
| donation | `donated == 0` | which of four causes |

Proxies are where this hides. The standing question for any new criterion:
**what observation would make this FAIL, and is that observation in
scope?** If the answer is "the thing we measure is downstream of the thing
we care about", the gate cannot fail for its own stated reason.

**Citation rules**, derived from ~190 machine-checked citations across five
agents (19 range slips, 1 wrong path, 0 fabrications):

1. Anchor to `grep`, never to a position counted inside a rendered read —
   elisions silently absorb lines.
2. Escape regex metacharacters when searching literal code; unescaped
   parens return confident false negatives.
3. Cite `git grep … HEAD`, not the working tree, when the artifact is
   durable.
4. Verify against content, never against the message claiming it.
5. Cite what survives the refactor — cite the definition site, or by
   symbol.

Corollary: **a negative needs a control.** "Absent from file A" is evidence
only once the same pattern is shown to match in sibling file B.

**Concurrency:** `git commit --only -F <msg> -- <paths>` protects other
agents' *other files*. Nothing at the git level protects a *shared* file —
`--only` commits that path's full worktree content. Shared files need a
claim, not a flag. Three index sweeps today, all on
`docs/releases/v0.8.0-notes.md`, the one file nobody claimed.

**Four checkouts of this repo exist on the box.** A relative
`libs/mlx-swift-lm/...` resolves to a real but two-PRs-stale tree. Use
absolute paths.
